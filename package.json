{
  "name": "@animando/stompjs-redux-saga",
  "version": "1.0.0",
  "description": "Turns a callback into a redux saga",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:ts": "rm -rf build",
    "clean": "npm run clean:ts",
    "build": "npm run clean && npm run build:ts",
    "build:ts": "tsc -p ."
  },
  "keywords": [],
  "author": "Graham King",
  "license": "MIT",
  "dependencies": {
    "@animando/redux-saga-callback": "^1.0.5",
    "@stomp/stompjs": "^5.4.3",
    "redux-actions": "^2.6.5",
    "redux-saga": "^1.1.3",
    "sockjs-client": "^1.4.0"
  },
  "peerDependencies": {},
  "devDependencies": {
    "@types/redux-actions": "^2.6.1",
    "@types/sockjs-client": "^1.1.1"
  }
}
